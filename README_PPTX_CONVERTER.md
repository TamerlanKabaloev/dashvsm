# PPTX → PDF Конвертер с поддержкой видео

Скрипт для конвертации презентаций PowerPoint (PPTX) в PDF с сохранением видео файлов.

## ⚠️ Важные ограничения

**PDF формат имеет ограничения для видео:**
- PDF не поддерживает автоматическое проигрывание видео
- Видео можно воспроизводить только интерактивно (по клику)
- Видео файлы сохраняются отдельно и должны находиться в той же папке, что и PDF
- Для проигрывания требуется PDF-ридер с поддержкой мультимедиа (Adobe Acrobat Reader, Foxit Reader и др.)

## 📦 Установка

```bash
pip install -r requirements_pptx_converter.txt
```

## 🚀 Использование

### Базовое использование

```bash
python pptx_to_pdf_converter.py presentation.pptx
```

Это создаст файл `presentation.pdf` в той же папке.

### Указание выходного файла

```bash
python pptx_to_pdf_converter.py presentation.pptx output.pdf
```

## 📋 Что делает скрипт

1. **Извлекает видео** из PPTX файла (MP4, AVI, MOV, WMV и др.)
2. **Конвертирует слайды** в PDF страницы
3. **Создает интерактивные элементы** для воспроизведения видео
4. **Копирует видео файлы** рядом с PDF для доступа

## 🎬 Воспроизведение видео в PDF

После конвертации:

1. **Видео файлы** будут скопированы в ту же папку, что и PDF
2. Откройте PDF в **Adobe Acrobat Reader** или другом PDF-ридере с поддержкой мультимедиа
3. На слайдах с видео будут **кнопки "▶ Воспроизвести видео"**
4. Кликните на кнопку для воспроизведения видео

### Поддерживаемые PDF ридеры

- ✅ Adobe Acrobat Reader (рекомендуется)
- ✅ Foxit Reader
- ✅ PDF-XChange Viewer
- ⚠️ Стандартные PDF просмотрщики (Chrome, Edge) - ограниченная поддержка

## 🔧 Технические детали

- Использует `python-pptx` для чтения PPTX файлов
- Использует `reportlab` для создания PDF с интерактивными элементами
- Извлекает видео из ZIP-архива PPTX (так как PPTX это ZIP)
- Создает файловые аннотации для связи PDF с видео

## ⚙️ Альтернативные решения

Если вам нужно **автоматическое проигрывание** видео:
- Используйте **PowerPoint → Видео** (File → Export → Create Video) для создания MP4
- Или используйте онлайн-конвертеры в видео формат

Если видео не критично:
- Используйте стандартную конвертацию через PowerPoint (File → Save As → PDF)
- Это создаст статический PDF без видео

## 🐛 Устранение проблем

**Проблема:** Видео не воспроизводится
- Убедитесь, что видео файлы находятся в той же папке, что и PDF
- Используйте Adobe Acrobat Reader
- Проверьте, что видео файлы не повреждены

**Проблема:** Некоторые элементы не отображаются правильно
- Сложные анимации и переходы не сохраняются в PDF
- Некоторые фигуры могут отображаться упрощенно
- Текст может не сохранить все форматирование

**Проблема:** Ошибка при установке зависимостей
```bash
# Установите зависимости по отдельности
pip install python-pptx
pip install reportlab
pip install Pillow
```

## 📝 Пример использования в коде

```python
from pptx_to_pdf_converter import PPTXToPDFConverter

# Конвертация
with PPTXToPDFConverter('presentation.pptx', 'output.pdf') as converter:
    converter.convert()
```

## ⚡ Производительность

- Скрипт обрабатывает слайды последовательно
- Большие презентации могут занять время
- Видео файлы копируются, что может увеличить время для больших файлов

## 📄 Лицензия

Скрипт предоставляется "как есть" для использования.

