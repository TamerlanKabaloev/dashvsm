# 📝 Инструкция: Как сделать git push

## Вариант 1: Если у вас еще НЕТ git репозитория

### Шаг 1: Инициализация git

Откройте терминал (PowerShell или Command Prompt) в папке проекта и выполните:

```bash
git init
```

### Шаг 2: Добавление файлов

```bash
git add dashboard.py pricing_matrix.py requirements.txt .streamlit/config.toml README.md DEPLOY.md
```

Или добавить все файлы (кроме игнорируемых):
```bash
git add .
```

### Шаг 3: Первый коммит

```bash
git commit -m "Initial commit: Pricing Matrix App"
```

### Шаг 4: Создание репозитория на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите кнопку "+" в правом верхнем углу → "New repository"
3. Назовите репозиторий (например: `pricing-matrix-app`)
4. Выберите "Public" или "Private"
5. **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
6. Нажмите "Create repository"

### Шаг 5: Подключение к GitHub и отправка

После создания репозитория GitHub покажет инструкции. Выполните:

```bash
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
```

Замените:
- `YOUR_USERNAME` - ваш логин на GitHub
- `REPO_NAME` - название вашего репозитория

Например:
```bash
git remote add origin https://github.com/ivanov/pricing-matrix-app.git
```

Затем:
```bash
git branch -M main
git push -u origin main
```

При первом push GitHub попросит авторизоваться. Введите логин и пароль (или токен доступа).

---

## Вариант 2: Если у вас УЖЕ ЕСТЬ git репозиторий

### Просто обновите код:

```bash
# Добавить все изменения
git add .

# Создать коммит
git commit -m "Update pricing matrix app"

# Отправить на GitHub
git push
```

---

## Вариант 3: Если репозиторий уже подключен, но нужно обновить

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Закоммитить
git commit -m "Описание изменений"

# Отправить
git push
```

---

## Полезные команды

### Проверить статус:
```bash
git status
```

### Посмотреть историю коммитов:
```bash
git log
```

### Посмотреть подключенные репозитории:
```bash
git remote -v
```

### Если нужно изменить URL репозитория:
```bash
git remote set-url origin https://github.com/YOUR_USERNAME/REPO_NAME.git
```

---

## Решение проблем

### Ошибка: "fatal: not a git repository"

Выполните:
```bash
git init
```

### Ошибка: "remote origin already exists"

Если нужно изменить URL:
```bash
git remote set-url origin https://github.com/YOUR_USERNAME/REPO_NAME.git
```

### Ошибка авторизации

GitHub больше не принимает пароли. Нужно использовать токен доступа:

1. Перейдите: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите "Generate new token"
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. При push используйте токен вместо пароля

Или настройте SSH ключ (более безопасно).

---

## После успешного push

1. Перейдите на [share.streamlit.io](https://share.streamlit.io)
2. Войдите через GitHub
3. Нажмите "New app"
4. Выберите ваш репозиторий
5. Укажите:
   - Branch: `main`
   - Main file path: `dashboard.py`
6. Нажмите "Deploy!"

Готово! 🎉

