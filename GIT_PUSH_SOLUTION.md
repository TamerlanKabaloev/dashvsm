# 🔧 Решение проблемы с git add

## Проблема
Папка `test_mcp/` является git репозиторием, поэтому `git add .` не работает.

## Решение

### Вариант 1: Добавить файлы по отдельности (рекомендуется)

Выполните в терминале:

```bash
# Добавить .gitignore (чтобы исключить test_mcp)
git add .gitignore

# Добавить основные файлы приложения
git add dashboard.py pricing_matrix.py requirements.txt

# Добавить конфигурацию
git add .streamlit/config.toml

# Добавить документацию
git add README.md DEPLOY.md GIT_INSTRUCTIONS.md

# Создать коммит
git commit -m "Add pricing matrix dashboard app"

# Отправить на GitHub
git push -u origin master
```

### Вариант 2: Добавить все кроме test_mcp

```bash
# Добавить .gitignore
git add .gitignore

# Добавить все файлы (test_mcp будет проигнорирован благодаря .gitignore)
git add .

# Создать коммит
git commit -m "Add pricing matrix dashboard app"

# Отправить на GitHub
git push -u origin master
```

### Вариант 3: Если нужно добавить test_mcp как подмодуль (не рекомендуется)

Если `test_mcp` нужен в репозитории, добавьте его как git submodule:

```bash
git submodule add <URL_РЕПОЗИТОРИЯ_test_mcp> test_mcp
```

Но для Streamlit Cloud это не нужно, лучше просто исключить эту папку.

## После успешного push

1. Перейдите на [share.streamlit.io](https://share.streamlit.io)
2. Войдите через GitHub
3. Нажмите "New app"
4. Выберите репозиторий: `Tamerlankabaloev/dashvsm`
5. Укажите:
   - Branch: `master`
   - Main file path: `dashboard.py`
6. Нажмите "Deploy!"

