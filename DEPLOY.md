# 🚀 Инструкция по публикации на Streamlit Cloud

## Подготовка к публикации

### 1. Проверьте файлы проекта

Убедитесь, что у вас есть следующие файлы:
- ✅ `dashboard.py` - главный файл приложения
- ✅ `pricing_matrix.py` - модуль с логикой
- ✅ `requirements.txt` - зависимости
- ✅ `.streamlit/config.toml` - конфигурация (опционально)

### 2. Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Назовите репозиторий (например: `pricing-matrix-app`)
4. Выберите "Public" или "Private"
5. **НЕ** добавляйте README, .gitignore или лицензию (если файлы уже есть локально)
6. Нажмите "Create repository"

### 3. Загрузите код в GitHub

Если у вас еще нет git репозитория:

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление файлов
git add dashboard.py pricing_matrix.py requirements.txt .streamlit/config.toml README.md

# Первый коммит
git commit -m "Initial commit: Pricing Matrix App"

# Добавление удаленного репозитория (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Отправка в GitHub
git branch -M main
git push -u origin main
```

Если репозиторий уже существует:

```bash
git add .
git commit -m "Add Streamlit pricing matrix app"
git push
```

## Публикация на Streamlit Cloud

### Шаг 1: Вход в Streamlit Cloud

1. Перейдите на [share.streamlit.io](https://share.streamlit.io)
2. Нажмите "Sign in"
3. Войдите через ваш GitHub/GitLab/Bitbucket аккаунт
4. Разрешите доступ Streamlit к вашему аккаунту

### Шаг 2: Создание нового приложения

1. Нажмите "New app"
2. Заполните форму:
   - **Repository**: выберите ваш репозиторий из списка
   - **Branch**: `main` (или `master` если используете старую ветку)
   - **Main file path**: `dashboard.py`
   - **App URL**: выберите уникальное имя (например: `pricing-matrix-app`)
     - URL будет: `https://pricing-matrix-app.streamlit.app`
3. Нажмите "Deploy!"

### Шаг 3: Ожидание деплоя

Streamlit Cloud автоматически:
- ✅ Клонирует ваш репозиторий
- ✅ Установит зависимости из `requirements.txt`
- ✅ Запустит `streamlit run dashboard.py`
- ✅ Предоставит публичный URL

Обычно это занимает 1-3 минуты.

### Шаг 4: Проверка приложения

1. После успешного деплоя вы увидите зеленую галочку ✅
2. Нажмите на URL приложения или кнопку "Manage app"
3. Откройте приложение в браузере
4. Проверьте, что все функции работают

## Обновление приложения

После каждого изменения в коде:

```bash
git add .
git commit -m "Update app"
git push
```

Streamlit Cloud автоматически перезапустит приложение с новыми изменениями (обычно через 1-2 минуты).

## Настройки приложения

### Изменение URL

1. Перейдите в "Manage app"
2. Нажмите "Settings"
3. Измените "App URL"
4. Сохраните изменения

### Переменные окружения (если нужны)

1. В "Manage app" → "Settings"
2. Добавьте "Secrets" для конфиденциальных данных
3. Или используйте "Environment variables"

## Устранение проблем

### Приложение не запускается

1. Проверьте логи в "Manage app" → "Logs"
2. Убедитесь, что `requirements.txt` содержит все зависимости
3. Проверьте, что путь к главному файлу указан правильно

### Ошибки импорта

- Убедитесь, что все модули (например, `pricing_matrix.py`) находятся в корне репозитория
- Проверьте, что все зависимости указаны в `requirements.txt`

### Медленная загрузка

- Streamlit Cloud предоставляет бесплатный тариф с ограничениями
- Для более быстрой работы рассмотрите платный тариф

## Полезные ссылки

- [Документация Streamlit Cloud](https://docs.streamlit.io/streamlit-community-cloud)
- [Streamlit Community Cloud](https://share.streamlit.io)
- [Документация Streamlit](https://docs.streamlit.io)

